# check_mk_jvm_gc_mem
Service that takes memory gc before and after values and saves it on check_mk for graphs. In this version there's not any thresshold, but it can be configurable with if statement.
